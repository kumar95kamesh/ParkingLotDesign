package strategies;

import models.Gate;
import models.ParkingSpot;
import models.enums.ParkingSpotStatus;
import models.enums.VehicleType;
import repositories.ParkingSpotRepository;

public class NearestSpotAssignmentStrategy implements SpotAssignmentStrategy{

    ParkingSpotRepository parkingSpotRepository;

    @Override
    public ParkingSpot assignSpot(VehicleType vehicleType, Gate gate) {

        //write some basic code which gives a valid empty parking spot


        //Homework:- Please weite some basic code which should return a valid parking spot
        return null;

    }
}
